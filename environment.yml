name: boule
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.7
    - pip
    # Required
    - numpy
    - attr
    # Optional
    # Development
    - pytest
    - pytest-cov
    - coverage
    - black
    - pylint
    - flake8
    - sphinx=2.2.1
    - sphinx_rtd_theme=0.4.3
    - sphinx-gallery
